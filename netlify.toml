# Sentinel — production deployment (Netlify + Next.js)

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
